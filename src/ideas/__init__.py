"""Генератор идей блюд.

`palette.py`   — что модель имеет право использовать (чистый Python по ING);
`generator.py` — вызов модели, разбор ответа, расчёт UC по каждому варианту.

Разделение принципиальное: модель придумывает НАЗВАНИЕ, КОНЦЕПЦИЮ и СОСТАВ,
все деньги и КБЖУ считает калькулятор (`src/calc/costs.py`). Правило проекта
«числа считает калькулятор, не LLM» здесь не смягчается — оно тут главное.
"""
